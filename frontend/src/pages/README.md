# Pages
Route-level components that compose layouts and features.
- Keep these lean; they should mostly handle data orchestration and layout selection.
- Supports lazy loading via React Router's `lazy` or dynamic imports.
