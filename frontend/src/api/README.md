# API Layer

Contains global API client configuration, base instances (e.g., Axios), and shared API-related types or interceptors.

- `client.js`: Base configuration for API requests.
- `endpoints.js`: Centralized endpoint definitions.
